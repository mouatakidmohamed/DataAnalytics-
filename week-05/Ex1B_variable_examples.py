# Description: This script practices creating variables with good names
# Author: Mohamed Mouatakid

customer_id = 1001 # I used a number because customer IDs are usually numeric, but some systems may use letters too.
customer_name = "Mohamed Mouatakid" # I used first name + last name, but they could also be stored separately.
customer_gender = "Male" # I used text, but a real system may use options like M, F, or another value.
customer_date_of_birth = "2000-01-01" # I used a string in YYYY-MM-DD format to keep the date clear.
driver_license_number = "D123456789" # I used a string because license numbers can include letters and numbers.
auto_policy_number = "AP-987654" # I used a string because policy numbers can include letters, numbers, and symbols.

my_name = "Mohamed Mouatakid"
birth_city_state = "Tangier, Morocco"

# Reserved words in Python:
# False, None, True, and, as, assert, async, await, break, class, continue,
# def, del, elif, else, except, finally, for, from, global, if, import,
# in, is, lambda, nonlocal, not, or, pass, raise, return, try, while,
# with, yield

# if: Used to test a condition and run code only when that condition is true.
# else: Used to run a different block of code when the if condition is false.
# for: Used to create a loop that repeats for each item in a sequence.
# while: Used to create a loop that continues while a condition is true.
# import: Used to bring code from another module into the current program.
